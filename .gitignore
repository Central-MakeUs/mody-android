*.iml
.gradle
.idea/
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
local.properties

# Kotlin
.kotlin/

# Build outputs
release/
# 단, release 는 빌드 산출물 폴더명이자 변형별 소스셋 이름이기도 하다 — 소스는 추적한다.
!*/src/release/
!*/*/src/release/
*.apk
*.aab

# Signing (절대 커밋 금지)
*.jks
*.keystore
keystore.properties

# Figma 추출 산출물 — 시안이 바뀌면 매번 다시 뽑는다(재생성 가능).
# 매핑 테이블·스펙 문서는 사람이 검수한 결과라 추적한다.
tools/figma/out/
