*.iml
.gradle
.idea/
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
local.properties

# Kotlin
.kotlin/

# Build outputs
release/
# 단, release 는 빌드 산출물 폴더명이자 변형별 소스셋 이름이기도 하다 — 소스는 추적한다.
!*/src/release/
!*/*/src/release/
*.apk
*.aab

# Signing (절대 커밋 금지)
*.jks
*.keystore
keystore.properties
# 예외: debug 전용 키스토어. 팀·CI 가 같은 서명을 써야 카카오 키 해시가 고정된다.
# 비밀번호가 안드로이드 기본값(android)인 공개 키스토어이고, 릴리스 서명키와 무관하다.
!app/debug.keystore
