package com.makeus.mody.feature.record.health.contract

import com.makeus.mody.core.commonui.base.UiState

// TODO(record): 운동 기록 시안 확정 후 필드 추가
data class RecordHealthState(
    val placeholder: Unit = Unit,
) : UiState
